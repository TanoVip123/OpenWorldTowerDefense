// For now, SelectableComponent only informs if an entity can be selected.
// In the future, we can expand this component to include additional data related to selection, such as selection priority, visual indicators, or interaction options.
public struct SelectableComponent : IComponent { }
